const person = {
    name: 'liminjun',
    age: 30,
    hobbies: ['sports','cooking']
};

let activities: string[];

activities = ['sports'];

for (const hobyy person.hobbies) {
    console.log(hobyy);
    console.log(hobyy.map());
}
console.log(person.name);